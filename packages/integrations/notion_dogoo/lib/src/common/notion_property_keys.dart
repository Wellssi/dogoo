/// Keys used in Notion API objects.
abstract class NotionPropertyKeys {
  /// Property key represents the `avatar_URL` key in Notion
  static const String avatarUrl = 'avatar_url';

  /// Property key represents the `created_timestamp` key in Notion
  static const String createdTime = 'created_time';

  /// Property key represents the `id` key in Notion
  static const String id = 'id';

  /// Property key represents the `last_edited_timestamp` key in Notion
  static const String lastEditedTime = 'last_edited_time';

  /// Property key represents the `name` key in Notion
  static const String name = 'name';

  /// Property key represents the `object` key in Notion
  static const String object = 'object';

  /// Property key represents the `type` key in Notion
  static const String type = 'type';
}
